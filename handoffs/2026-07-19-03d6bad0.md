# Handoff — 2026-07-19

session_id: 03d6bad0-4e9d-4ebb-8bc6-c5ba92782ec5
purpose: Root-cause + fix + ship bds-mifubu (false-empty /batterie:update snapshot); recover and unblock bds-joroza
format: fond-v1

## For the next Claude

### Done
- **bds-mifubu closed, fix shipped as suite 1.15.1** (assemble green, verified in the installed 1.15.1 cache). Root cause was NONE of the brief's three hypotheses exactly: `known_marketplaces.json` records a URL-added marketplace as `{source:"git", url:"https://github.com/spm1001/batterie.git"}` — not the `{source:"github", repo:"o/r"}` shorthand the discovery in `/batterie:update` + `/batterie:version` read. Tube's public marketplace resolved to `""` → not family; the `or {"batterie"}` fallback was defeated because batterie-home (shorthand shape) kept `fam` non-empty. Deterministic, reproduced in one command by running the extracted snapshot script against the live registry. Fix: `source_repo()` normalises both shapes (https + ssh URL forms); registry read failure, unresolved family-named marketplaces, and the empty-registry-with-cache-present (bds-wezubo drop signature) each render loud, distinct messages; prose "snapshot sanity gate" added to the skill. 13 new regression checks, each seen failing pre-fix; 34/34 green; both scripts re-verified against tube's real registry.
- **bds-joroza recovered and fully unblocked.** Its stale tactical (claimed 2026-07-13, session a42c0649) was cleared to free the cwd; the step-1 finding — which lived ONLY in that session's transcript (it wrote no handoff) — was dug out via deglacer and banked into the brief: clone-and-go is viable (CC runs cloned-in `.claude/settings.json` hooks after the ordinary one-time folder-trust prompt, no separate hook gate, verified interactive + print on v2.1.207). Both design forks resolved with Sameer this session: (2) unattended auto-push to garden main = blessed; (1) identity = GitHub login of the pushing credential (`gh api user -q .login`, fallback git config email) mapped to plot name via a garden-root `gardeners.toml` seeded from mit-commons `TEAM.md` (plots are first-name-keyed — `raw/sameer` vs `spm1001` — so the mapping is required; `.garden-self`/`GARDEN_SELF` as override; unresolvable = loud + skip push). Full detail in the brief's `--how`.
- understanding.md updated (marketplace source-shape lesson, in the Publish/Update section); suite CHANGELOG carries the 1.15.1 entry.

### Reflection
- The filing session's hypotheses (race, exception-fallthrough) were plausible and wrong; hypothesis (a) was close but pointed at the wrong file. What settled it in minutes was reproducing the generator as-shipped against the real substrate before theorising — extract the `!`-block, run it, dissect the filter chain. Reproduce-first beat hypothesis-triage.
- Honest wobble: my first joroza breadcrumb attributed the stale tactical to c8e301c9 (the session whose handoff I'd just read) — plausible recall written durably without verification; the transcript dig showed the claimant was actually a42c0649, a parallel session that morning. Ledger #19's shape (recall masquerading as provenance in verdict-adjacent prose), caught and corrected in-session.
- Sameer's "anything else we can knock out quickly?" is what surfaced the joroza recovery — the finding was one `bon work --clear` from invisible. The quick-tiddler question is itself a recovery instrument.

### Uncertain
- Whether other machines (Mac, hezza) also carry git+url-shaped registry entries — irrelevant to correctness now (1.15.1 handles both shapes), never checked.

### Risks
- `skills/update/SKILL.md` and `skills/version/SKILL.md` deliberately duplicate the discovery block (separate `!`-embedded scripts). Drift guard: `tests/test_marketplace_aware.py` extracts and exercises BOTH through the same fixtures — keep it that way when editing either.
- The wezubo cache-vs-registry check keys on `~/.claude/plugins/cache/batterie/` existing; if CC's cache layout changes it silently lapses. Accepted: it's a warning layer, not the primary rendering.
- The suspicious-marketplace warning keys on family-style *names* — a future unrecognised source shape under a non-family name still drops silently. Accepted as the warning layer's honest limit; the fix layer (source_repo) is shape-based.

### Opportunities
- **bds-joroza is the ripest pick** — steps 2–6, with the trust question answered, both forks resolved, and the identity design in the brief. Step-4 gotcha already banked: estate `~/.gitignore_global` ignores `.claude/`, so committing the garden hooks needs one-time `git add -f` or a repo-level un-ignore.
- bds-devece (deglacer CLI smoke tests) — small, the last open action under the done depemo outcome.
- bds-naceje / bds-pabajo — the two real builds still standing on the board.
- CC sessions that die without /close leave findings only in transcripts (tonight's near-loss). That's bds-lomefi/overnight-digest territory when it gets picked up — the recovery move below is the interim.

## For Claudes to come

**Fixture monoculture is how a well-tested filter still ships a total failure.** The marketplace discovery had a real test suite — but every fixture modelled the `github`+`repo` shorthand, the only shape its author would have written by hand. CC's registry also records `{source:"git", url:"…github.com/o/r.git"}` for URL-based adds (and `{source:"directory", path:…}` for local dev), and that shape persists across marketplace updates. When code filters on a field of a registry *someone else writes*, enumerate the shapes the registry actually records — read a real one from another machine or era, don't just fixture the shape you'd write. Corollary shipped with the fix: when a filter yields empty against a non-empty input, say so loudly and distinctly — "genuinely none" and "my filter recognised none" must never share a rendering.

**A session that ends without /close strands its findings in the transcript — and they're recoverable in minutes.** Session a42c0649 (2026-07-13) resolved joroza's load-bearing trust question and wrote no handoff; the only durable trace was a tactical ✓ that a later `bon work --clear` (this session, needing the cwd) would have erased. The recovery move, deglacer-gated: grep `~/.claude/projects/*/[a-f0-9]*.jsonl` for a unique term (the bon ID), rank by mention count, then extract assistant text with jq and read the tail — the session's own conclusions live there verbatim. Budget ~5 minutes. When clearing another session's tactical, assume its findings may exist nowhere else: breadcrumb first, and if the step mattered, dig before the knowledge goes cold.
