# Handoff — 2026-06-20

session_id: 34893c81-90e7-4eb1-baa6-2724727f61a4
purpose: Fixed the failing lint CI, then ran a suite-wide post-cutover staleness sweep across all 6 source repos — shipped the doc/version fixes, cleared a ratchet quarantine, brought the marketplace and this machine current
format: fond-v1

## For the next Claude

### Done
- **Fixed the failing lint CI** (bds, `1becb3e`). Removed the broken cross-repo `batterie-lint.py` step: since the 2026-06-10 cutover it needs every source repo checked out as a sibling (reads `marketplace.json` from the `spm1001/batterie` clone), which only the assemble workflow provides — so it can't run in this repo's single-repo checkout. Had been red on **every push for 10 days**, silently. Left a guard comment; the lint still runs in full in the assemble workflow.
- **Destaled the bds repo** (`9acd892`). Rewrote `understanding.md` to post-cutover reality (it described this repo AS the marketplace); fixed `plugin-capabilities.md` (harmful install rows that 404, friction T1 marked resolved, dated-snapshot banner over the March matrix), `fond-architecture.md` (superseded banner — garde decommissioned), `CLAUDE.md` (`/batterie-update`→`/batterie:update`). **bds-goluce closed.**
- **Fixed `spm1001/batterie/CLAUDE.md`** org-marketplace landmine (`625d2dd`). It claimed the org marketplace "syncs from this repo (Sync-automatically is on)" — false since bds-kanuve (org Directory removed, repo deliberately public). Carried the don't-re-add / don't-flip-private warning into the artifact repo where it's most load-bearing.
- **Suite-wide staleness sweep** — 5 parallel auditors (one per source repo) → fix → publish. Shipped: **mise 0.7.12, trousse 0.5.12, passe 0.6.4, todoist-gtd 0.4.8**; **bon** commit-only (`e7e4d88`, no vendored change). Fixed garde-manger refs, stale marketplace nav paths (`batterie-de-savoir`→`batterie`), dead `uv tool install` forms, `~/Repos` casing, trousse's drifted skills table (3 dead skills, 3 missing, wrong count), and trousse's plugin.json **description** ("Best with: garde-manger" — the marketplace-facing blurb, caught by reading the file, not by the auditors).
- **Cleared a ratchet quarantine** (`db482f4`, batterie 1.1.1→1.1.2): my own `9acd892` CLAUDE.md edit had drifted the batterie plugin's vendored content without a bump. The assemble caught it (healthy plugins shipped, batterie held). Marketplace assembled green at 1.1.2 (commit 00c81a5).
- **Ran `/batterie:update`** + reinstalled the two drifted CLIs (passe 0.6.4, todoist 0.4.8). hezza fully current; **suite v1.1.2**.
- Updated **bds-bidasi** with the third silent-failure surface. Filed: **tgt-kobale** (todoist token-dir code fix, deferred), **bds-naceje** (doc-drift resistance), and first actions on three empty outcomes — **bds-kubufa** (laliru / PR-gate skill), **bds-cocusa** (bomufu / valence-pattern catalogue), **bds-vegowo** (wezubo / registry self-heal guard).

### Reflection
- The verification reflex held under high-volume mechanical churn — exactly where it's easiest to drop. It caught: an auditor's *backwards* Python-version suggestion (pyproject is 3.11; applying it would have introduced an error), a `head -20` that made a good commit *look* like it dropped the SKILL.md fix, and my own "live functional bug" overclaim on the todoist token-dir — corrected by tracing the code flow (it's stale, not broken: the legacy-file fallback catches it).
- The melobo-stop fired cleanly on tgt-kobale: the sweep was billed as docs, the todoist token-dir turned out to be token-handling *code* with migration implications, so I handed you the fork rather than doing token surgery at a long session's tail.

### Risks
- Hand-maintained doc surfaces (per-repo README skill tables, plugin-capabilities matrix) drifted badly and will again — nothing lints doc-vs-reality. Tracked as **bds-naceje** (generate, like for-agents.md, or lint).
- **tgt-kobale**: todoist's token-dir name is stale in *code* (`token_store.py`), so the intended version-stable plugin-data storage is silently disabled — it falls back to `~/.todoist-token` (works, not as designed). Migration-safe design is in the brief; do it as a fresh-session opener.

### Opportunities
- **bds-bidasi** — suite cron/CI failure alerting. The session's standout next step: silent failures bit us 3× today (lint CI 10 days red, the assemble quarantine only caught by watching live, the general no-alerting gap). Matters most as ITV onboarding scales.
- **tgt-kobale** — the todoist token-dir code fix; best as a fresh, careful opener.
- Security triage before broad onboarding — **passe-kabola** (3 high Dependabot), **mise-dumebo** (git-tracked OAuth posture).
- **bds-naceje** — kill the doc-drift class so this sweep doesn't recur.
- **bds-hifusu** stays Someday — gated on the Cornichon-reshape direction (the mise-in-Cornichon decision), which is yours to make.

## For Claudes to come

**This repo is a *source*, not just the umbrella — editing its `CLAUDE.md`/`skills/`/`hooks/`/`instructions.md` silently drifts the suite-level `batterie` plugin and needs a `plugin.json` bump.** A "docs" edit here feels free, but those files are vendored into the batterie plugin, so the assemble ratchet quarantines batterie if you don't bump. This exact miss has fired at least three times (CHANGELOG 0.2.2 on 2026-06-11, 1.1.2 on 2026-06-20). Rule of thumb: `docs/` and `.bon/` edits are genuinely free; `CLAUDE.md`/`instructions.md`/skill/hook edits need a bump. (Now in understanding.md's Assembly Bus section.)

**Suite-wide staleness recurs *because* the suite is multi-repo — fan-out auditing is the cheap sweep, but verify each auditor's fixes before applying.** Post-cutover staleness (this repo no longer being the marketplace; garde-manger/tafelmusik decommissioned; install-path + marketplace-name changes) scatters across all 6 repos in understanding.md, CLAUDE.md, README, SKILL.md, *and* plugin.json descriptions. One auditor per repo, each handed a post-cutover fact-sheet plus a vendored/not-vendored rule, found it fast and cheaply. But treat auditor *fixes* as weak evidence: one confidently suggested reconciling a correct Python version to a wrong one, and the auditors missed the plugin.json `description` field entirely (give a future audit prompt the whole plugin.json, not just SKILL.md frontmatter). The durable fix is to stop hand-maintaining the rot-prone surfaces — generate them from the registry/skills dir (like `for-agents.md`) or lint them; tracked as bds-naceje.
