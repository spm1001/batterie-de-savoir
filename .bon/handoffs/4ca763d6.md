# Handoff — 2026-03-31

session_id: 4ca763d6-ef93-4923-b6ef-66ec725f124f
purpose: Triage backlog, build cross-repo lint, slim marketplace

## Done
- Triaged bon backlog: 11→2 outcomes (closed 9 as wrong-repo/done/stale/duplicate)
- Built scripts/batterie-lint.py — 5 alignment checks (skill routing, hook registration, version consistency, instruction shards, executability)
- Added weekly CI schedule (Monday 07:00 UTC) for cross-repo drift detection
- Converted /batterie:batterie-update → /batterie:update (commands/ → skills/ migration)
- Created ensure-passe.sh hook + wired into plugin.json (committed and pushed to passe repo)
- Removed guéridon from marketplace, brigade.toml, all docs, Mermaid diagram
- Fixed smoke test expected skill names to match actual CC discovery output (directory names, not frontmatter names)
- Bumped batterie version to 0.1.7
- Filed bds-sucega: converge CLI+Desktop to single spm1001/batterie marketplace
- Cloned spm1001/batterie (Desktop marketplace) to hezza

## Gotchas
- CC surfaces skills by DIRECTORY name, not frontmatter `name:` field. `/bon:open` not `/bon:bon`. The smoke test was wrong about this for every plugin.
- `commands/` is legacy — both commands and skills are loaded identically. Only difference: skills support references/ and scripts/ subdirectories.
- Context7 MCP was added to deny list in settings.json — user doesn't want it. (May have been reverted — check.)
- Passe plugin.json was bumped to 0.5.1 by another session's linter. Our ensure hook commit went on top of that.

## Risks
- Weekly CI clones 8 public repos — if any go private or rename, lint silently skips them
- batterie-lint.py doesn't validate the Desktop marketplace (spm1001/batterie) — only source repos
- Smoke test timed out on first run (only got through batterie/update before dying). May need optimisation or a shorter plugin list.

## Next
- One-repo convergence: make spm1001/batterie the single marketplace for CLI and Desktop (bds-sucega)
- Desktop/Cowork testing still open (bds-zotowo with 4 actions)
- Consomme .toml commands should migrate to skills/ format (user will handle in consomme repo)
- Smoke test needs a successful full run to validate the fixed expected names

## Reflection
**Claude observed:** The session was productive but sprawling — bon triage, lint tool, commands research, guéridon removal, Desktop marketplace all in one session. The thread-switching meant the one-repo convergence question (the user's real medium-term interest) only got captured as a bon, not designed. The lint tool and smoke test fixes were the highest-value work — they catch the exact class of silent-drift bug that keeps breaking sessions.
**User noted:** "I'm going crazy — so many moving parts." The suite has grown to the point where cross-cutting changes touch 5+ repos. Better assembly automation is the path forward, not monorepo.
