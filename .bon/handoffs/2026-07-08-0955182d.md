# Handoff — 2026-07-08

session_id: 0955182d-4082-44c8-9adf-a2f0bc6d2420
purpose: Team-platform evidence day — the mit-commons distribution spike, the scar harvest, and the update-propagation spike (bds-nupepe, bds-hazaso, bds-pozubo)
format: fond-v1

## For the next Claude

### Done
- **bds-nupepe CLOSED** — `ITV/mit-commons` exists (private, ITV org) and the whole CC distribution path is PROVEN live on tube: create → `claude plugin marketplace add ITV/mit-commons` → install → `/mit-commons:hello` answered in a fresh session. Zero Claude-specific auth: the machinery rides the ambient git credential helper, so per-teammate cost = standard private-ITV-repo access (gh login + SSO authorize, one-time). Result doc: `docs/team-platform-distribution-spike.md`. Tube clone: `~/repos/itv/mit-commons`. CREATED ≠ LAUNCHED — private and unannounced until seeded.
- **bds-hazaso CLOSED** — `docs/team-platform-design-lessons.md`: six incident families, each mapped incident → invariant → the design choice that deletes the failure class, ranked by class size (1: no assembler; 2: merge=shipped; 3: monorepo blast radius; 4: generate docs from substrate; 5: design for platform-owned failures; 6: port cheap guards verbatim).
- **bds-pozubo FILED + CLOSED same session** — the harvest's one live caveat, tested immediately: `claude plugin update` is VERSION-GATED on current CC. Content merged without a bump reaches the marketplace clone on refresh but never the version-keyed installed cache ("already at the latest version"); a bump is necessary and sufficient (0.0.2 delivered, sentinel verified in a fresh session). Consequence designed in: merge=shipped includes a CI auto-stamp of plugin.json per merge (ronuho's brief carries it; mit-commons README warns about the interim manual bump, shipped as 0.0.3).
- **Briefs fed:** bds-halonu question (a) marked ANSWERED with pointers; bds-ronuho's --how now carries all three inputs (distribution spike, scar harvest, update-propagation finding) — verified intact after write.
- **Docs healed:** environment.md's stale "tube ITV-org fetch is SAML-blocked" claim corrected (verified live three ways today) and pushed in ~/.claude; understanding.md gained the team-platform ground-truth section + the bds-bidasi alerting-chain principle from the 07-07 handoff.

### Reflection
- The spike-before-design rhythm carried the whole day: three empirical unknowns (private-SAML marketplace, onboarding cost, update gating) each died in ~20 minutes against the live substrate, where weeks of doc-reasoning had left them uncertain. The version-keyed cache path (`cache/mit-commons/mit-commons/0.0.1/`) telegraphed the pozubo answer before the probe confirmed it — reading the substrate's shape is itself evidence.
- Sameer's steer mattered twice: "you're on vertex billing right now so scars I guess" correctly routed the session away from halonu (WebSearch-dependent), and the /close arg names the baton (he starts halonu in a Teams session).
- One transient oddity, adjudicated benign/watch: a single `bon edit bds-ronuho` returned "Item not found", unreproduced seconds later; retried clean, content verified by read-back. If it recurs, field-report on bon's board. The read-back-after-write discipline is what caught it.

### Uncertain
- Whether Anthropic's plugin machinery will ever grow content-based (commit-keyed) update detection — would make the auto-stamp unnecessary. Not worth waiting for; design for today's version gate.

### Risks
- **mit-commons until the auto-stamp CI lands:** a merged content change without a plugin.json bump silently never reaches installed plugins. README warns; the blueprint (bds-ronuho) owns the permanent fix. Don't seed real content before the stamp exists, or every contributor inherits the manual-bump trap batterie just escaped.
- The mit-commons plugin + marketplace are now live user-scope installs on tube (spike residue, one tiny skill) — harmless, and deliberately left as the living proof + exemplar. `claude plugin uninstall mit-commons@mit-commons` if it ever needs to go.
- The bds board moved under parallel sessions today (items closing elsewhere mid-session) — normal multi-session reality; trust `bon list` over any session's memory of the board.

### Opportunities
- **bds-halonu** — prior-art survey, needs Teams (WebSearch). Sameer is starting this. Question (a) is pre-answered in the brief; remaining: (b) what org-less individual claude.ai accounts can consume (the GUI half of the team), (c) Gemini Spark watch item, plus the analogue table (Homebrew taps, Oh My Zsh, VS Code, Obsidian, Debian/nixpkgs, Backstage) and the internal-prior-art hunt the brief lists.
- **bds-mawuvu** — the glossary; leans on halonu's current-terms research, natural successor.
- **bds-ronuho** — the blueprint, runs LAST; its brief now carries every input produced so far. After halonu + mawuvu it's unblocked.
- **bds-rozica** — the batched vendored stale-refs sweep (includes this repo's CLAUDE.md ghost-outcome footer), rides the next publish.

## For Claudes to come

**The class-deletion table is the instrument that turns a scar archive into a design input.** For each incident family ask not "what guard catches this?" but "what architecture makes this failure class inexpressible?" — batterie's guards (parity, ratchet, quarantine, manifest invariant) are all artifacts of vendoring + version ceremony, and the team-platform review found that deleting the assembler and the human-remembered bump deletes the classes those guards exist to catch. The ranking criterion — size of failure class deleted — is what makes it a decision record rather than war stories. Corollary discipline for any blueprint: mark every remaining assumption with its verifying spike, and let no design section outrun its evidence; today three such spikes (marketplace-vs-SAML, onboarding cost, update gating) each settled in twenty minutes what documentation could not settle in weeks.
